# Evolution Log

## 2026-04-16

- Initialized the public template scaffold.
- Added a root `AGENTS.md` as the operating constitution.
- Added `rules/`, `agents/`, `skills/`, and `memory/` starter files.
- Kept the design convention-first so the system is useful before automation hooks exist.
